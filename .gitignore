# Firebase Admin SDK service account key — grants full admin access, never commit this
serviceAccountKey.json

# Installed npm packages — reinstalled via `npm install`, don't need to be in Git
node_modules/

# npm/Node debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (if you ever add a .env file for local secrets)
.env
.env.local

# OS-generated files
.DS_Store
Thumbs.db

# Editor/IDE folders
.vscode/
.idea/